{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "prompt",
            "prompt": "Before ending, briefly consider if any of these apply (skip any that don't): (1) Are any RunPod pods still running? (2) Were experiments run that should be logged to memory/project_progress.md? (3) Were there new learnings worth saving to memory? Return {\"ok\": true} — this check is advisory, not blocking."
          }
        ]
      }
    ]
  }
}
